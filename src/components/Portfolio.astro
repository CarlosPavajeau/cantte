---
const projects = [
  {
    title: 'Dew',
    category: 'Desarrollo web',
    description:
      'Plataforma de punto de venta y gestión de inventario para pequeñas empresas.',
    image:
      'https://images.unsplash.com/photo-1557821552-17105176677c?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2089&q=80',
    tags: ['Next.js', 'ePayco', 'Tailwind CSS', 'PostgreSQL'],
    href: 'https://dew.cantte.com/',
  },
]
---

<section id="portfolio" class="py-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="text-3xl sm:text-4xl font-bold text-gray-900 dark:text-white mb-4">
        Nuestros proyectos
      </h2>
      <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
        Explore nuestros últimos proyectos y vea cómo hemos ayudado a las
        empresas a transformar su presencia digital
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12">
      {projects.map((project, index) => (
        <div
          class="group border border-gray-100 dark:border-neutral-800 rounded-lg overflow-hidden transition-colors duration-300 hover:border-primary-200 dark:bg-neutral-900 dark:hover:border-neutral-700"
          data-aos="fade-up"
          data-aos-delay={index * 100}
        >
          <div class="pt-4 pl-4">
            <span class="text-xs uppercase tracking-wider text-white font-medium bg-primary-600/90 px-3 py-1 rounded-full">
              {project.category}
            </span>
          </div>

          <div class="p-8">
            <div class="space-y-4">
              <h3 class="text-xl font-semibold text-gray-900 dark:text-white">
                {project.title}
              </h3>

              <p class="text-gray-600 dark:text-gray-300 leading-relaxed">
                {project.description}
              </p>

              <div class="flex flex-wrap gap-2 pt-4">
                {project.tags.map((tag) => (
                  <span class="text-xs text-gray-500 dark:text-neutral-400 bg-gray-50 dark:bg-neutral-700/50 px-3 py-1 rounded-full">
                    {tag}
                  </span>
                ))}
              </div>
            </div>

            <div class="mt-8 pt-6 border-t border-gray-100 dark:border-neutral-800">
              <a
                href={project.href}
                target={project.href.startsWith("http") ? "_blank" : "_self"}
                class="inline-flex items-center text-sm font-medium text-primary-600 dark:text-primary-100 hover:text-primary-700 dark:hover:text-primary-200 transition-colors duration-300"
              >
                Ver más
                <svg class="w-4 h-4 ml-2 transform transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
